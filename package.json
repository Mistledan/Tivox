{
  "name": "tivox-token",
  "version": "1.0.0",
  "description": "TIVOX (TVX) ERC-20 token - fixed supply, burnable, ownable",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "deploy:base": "hardhat run scripts/deployBase.js --network base",
    "launch:base": "hardhat run scripts/launchBase.js --network base",
    "verify:sepolia": "hardhat verify --network sepolia",
    "verify:base": "hardhat verify --network base",
    "transfer:sepolia": "hardhat run scripts/transfer.js --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "dotenv": "^16.4.5"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
